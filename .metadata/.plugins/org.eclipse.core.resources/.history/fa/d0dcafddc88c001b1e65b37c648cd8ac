import View.GUIAngajat;
import View.GUIAutentificare;

import java.io.File;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Map;
import java.util.function.Function;
import java.util.stream.Collectors;

import javax.swing.JFileChooser;
import javax.swing.filechooser.FileSystemView;
import javax.xml.parsers.*;
import javax.xml.transform.*;
import javax.xml.transform.dom.*;
import javax.xml.transform.stream.*;
import org.xml.sax.*;

import Model.Angajat;
import Model.AngajatPersistenta;
import Model.Magazin;

import org.w3c.dom.*;
public class App {

	public static void main(String[] args) throws SAXException, IOException {
		//new GUIAngajat();
		new GUIAutentificare();
		/*AngajatPersistenta ang= new AngajatPersistenta();
		Angajat a = new Angajat("Alex","Alex13","passwa",21);
		Angajat aNou = new Angajat("Alexandra","Alexandra13","passwa",21);
		Angajat aNou2 = new Angajat("Bianca","BiaK","bianca",32);
		Magazin m = new Magazin("H&K");
		//ang.salvare(a, m);
		//ang.stergere(a, m);
		ang.actualizare(aNou, aNou2, m);*/
		/*File f = new File("F:\\Facultate_AN3SEM2\\PS\\Eclipse Work\\Magazin\\Images");
		JFileChooser j = new JFileChooser(f, FileSystemView.getFileSystemView());
		j.showSaveDialog(null);
		File f2=j.getSelectedFile().getAbsoluteFile();
		System.out.println(f2.toString());
		int lastSlash = f2.toString().lastIndexOf("\\");
		String imageName = f2.toString().substring(lastSlash+1,f2.toString().length());
		imageName.replace("\\", "/");
		System.out.println(imageName);*/
		
		/*String s1= "Zara";
		String s2= "HM";
		
		ArrayList<String> list = new ArrayList<String>();
		list.add(s1);
		list.add(s2);
		list.add(s1);
		
		Map<String,Long> rez = list.stream().collect(Collectors.groupingBy(Function.identity(),Collectors.counting()));
		rez.forEach((k,v) -> System.out.println(k + " " + v));*/
		
		
	}

}
